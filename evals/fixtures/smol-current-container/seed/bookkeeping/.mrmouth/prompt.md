# Smol Eval Agent Prompt

This is a tiny Mr Mouth eval. Keep the change intentionally small.

Follow the Litebrite item exactly. In the code worktree, edit only
`message.txt` so it contains exactly:

```text
hello from smol eval
```

Run `./check.sh` in the code worktree. Commit the code change in the code
worktree. Close the Litebrite task in the bookkeeping repo. Do not make
unrelated changes.
