# Multi-Do Epic Eval Agent Prompt

Work only on the current Litebrite leaf task.

Use `./SPEC.md` in the bookkeeping repo for shared behavior and `./worktree` for
code changes. Run the focused tests named in the current task before committing.
Commit code changes in `./worktree`, close only the current task unless the task
explicitly says otherwise, and avoid unrelated changes.
